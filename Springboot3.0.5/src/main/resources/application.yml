# 服务端配置
server:
  port: 8080

spring:
  application:
    name: springboot3.0.5

  # 数据库配置
  datasource:
    url: jdbc:mysql://localhost:3306/jwt
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.qztc.**.domain