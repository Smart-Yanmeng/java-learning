spring:
  application:
    name: gateway
  cloud:
    nacos:
      config:
        username: nacos
        password: nacos
        server-addr: 127.0.0.1:18849
        file-extension: yaml
        shared-configs:
          - data-id: common.yaml
        refresh-enabled: true
    gateway:
      discovery:
        locator:
          enabled: true
#      routes:
#        - id: student
#          predicates:
#            - Path=/student/**
#          uri: lb://student
#          filters:
#            - StripPrefix=1
#        - id: clazz
#          predicates:
#            - Path=/clazz/**
#          uri: lb://clazz
#          filters:
#            - StripPrefix=1
  profiles:
      active: test
