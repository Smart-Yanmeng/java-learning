spring:
  application:
    name: student
  cloud:
    nacos:
      config:
        username: nacos
        password: nacos
        server-addr: 127.0.0.1:18849
        file-extension: yaml
        shared-configs:
          - data-id: common.yaml
        refresh-enabled: true
  profiles:
      active: test
